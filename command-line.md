Command line is a direct window to the soul of your computer
ls lists all the folders inside other folders
ls -al lists all the hidden folders withing folders
cd takes you home
pwd shows what's inside the current directory/folder
mkdir creates a new folder
rm file removes or deletes a file
rm -r removes or deletes a directory
cp file copies a file
cp dir copies a directory
exit gets you out of the terminal